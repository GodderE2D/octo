// Command categories to map from command folder names to names for the help command.

const cmdCategories: Record<string, string> = {
  moderation: "Moderation",
  utils: "Utility",
};

export default cmdCategories;
